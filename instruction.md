settlement rollup is wrong again. `/app/workflow/export_report.py` isn't producing escalation output that matches what ops expects from `/app/data/events.json`, and nobody on the bridge has been able to pin down why.

build a CLI at `/app/log_audit.py` with `diagnose` and `repair` subcommands. diagnose works out what's actually wrong with the pipeline and writes a report; repair patches `/app/workflow/export_report.py` in place and regenerates the rollup outputs, writing `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json` and `repair_audit.json` into the output dir (`/app/output` by default). `/app/docs/report_spec.json` is the contract — every schema, field name, output path, and acceptance constraint that the CLI and the repaired pipeline have to satisfy is defined there, so match it exactly. leave the frozen snapshot at `/app/workflow/.export_report.original` read-only and intact; the diagnosis has to cite evidence from it.

months of bridge notes are in `/app/incident/export_dossier.md`. it's long, most of it is noise, and some of the early triage threads were contradicted by later findings — treat it as the source of truth for how the pipeline is *supposed* to behave, not as a list of facts that are each individually correct. every number you report has to be derived from the input data rather than hard-coded, and the repaired pipeline needs to keep working when it's pointed at a different events file. both the CLI and the patched pipeline must work purely from the operational files under `/app` — their code must never read, import or reference the verifier's `/tests` or `/solution` trees, at runtime or otherwise.
