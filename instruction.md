settlement rollup is wrong again. `/app/workflow/export_report.py` isn't producing escalation output that matches what ops expects from `/app/data/events.json`, and nobody on the bridge has been able to pin down why.

build a CLI at `/app/log_audit.py` with `diagnose` and `repair` subcommands. `diagnose --dossier PATH --report PATH` works out what's actually wrong with the pipeline and writes a report — its diagnosis has to cover all six known defects: `wrong_posted_field`, `priority_filter`, `recency_order`, `priority_normalization`, `dedupe_transaction`, and `waived_filter`. `repair --output-dir PATH` (default `/app/output`) patches `/app/workflow/export_report.py` in place and regenerates the rollup outputs, writing `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json` and `repair_audit.json` into the output dir. `/app/docs/report_spec.json` is the output contract and carries the full schema for every deliverable — the diagnosis report in both modes, the evidence rules, `repair_audit.json`, the summary fields and checksums, and the compact JSONL format for `flagged.jsonl` — so match it exactly rather than inferring shapes from the sample data, and read the whole file rather than the first screenful. one detail ops keeps tripping on: the repaired diagnosis `output_paths` keys are exactly `summary_json`, `flagged_jsonl` and `service_matrix_json` — the spec means it when it says not to substitute shorter aliases. `diagnose` reads its events from the fixed input path and takes no `--input` flag — passing `--input` to `diagnose` must fail with an argparse `unrecognized arguments: --input` error. leave the frozen snapshot at `/app/workflow/.export_report.original` read-only and intact; the diagnosis has to cite evidence from it.

what the spec deliberately does not tell you is how any of the values are *derived*. months of bridge notes are in `/app/incident/export_dossier.md`, and that's where the rollup's actual behavior got settled — normalization, input coercion, dedupe and its tie-breaks, waived handling, replay lineage, ordering. it's long, most of it is noise, and the early triage threads were revisited later and partly reversed, so read it as an argument that resolves over time rather than a list of facts that are each individually true. every number you report has to be derived from the input data rather than hard-coded, and the repaired pipeline needs to keep working when it's pointed at a different events file. apart from whatever input file it is explicitly pointed at, neither the CLI nor the patched pipeline may reach into the verifier's `/tests` or `/solution` trees — no referencing them in code, no reading anything out of them at runtime.
